import type { Config } from 'tailwindcss';
const config: Config = {
  content: ['./src/**/*.{ts,tsx}'],
  theme: { extend: {} },
  plugins: [],
};
export default config;
```
→ Commit changes

---

**File 6** — **"Add file" → "Create new file"** → tên:
```
tsconfig.json
